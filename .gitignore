# modelsim
work/
work_exp/
transcript
vsim.wlf
modelsim.ini

# vivado / xsim
.Xil/
xsim.dir/
xsim.covdb/
*.jou
*.log
*.pb
*.wdb

# generated by synth_check.tcl -- the parts worth keeping are echoed into
# results/synth_check.txt, these two are just where the tool had to put them
synth/utilization.txt
synth/cdc_report.txt

# hardware build output. hw/build.tcl regenerates all of it, and the project
# itself is built under TEMP because Vivado cannot write inside OneDrive.
hw/*.bit
hw/*.ltx
hw/ps7_init.*

# windows
Thumbs.db
desktop.ini

# Vivado writes a stray "NA" folder when the block design has no board
# summary path to write to
NA/

# intermediate render, make_pdf.py deletes it
docs/_report_raw.pdf

# personal interview notes, not part of the project
interview-prep.md

# vivado gui leaves these in the launch directory
vivado_pid*.str
vivado*.jou
vivado*.log
